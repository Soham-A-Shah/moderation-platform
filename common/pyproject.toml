[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "content-moderation-common"
version = "0.1.0"
dependencies = [
    "confluent-kafka==2.4.0"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["moderation_common*"]
